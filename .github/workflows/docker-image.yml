name: Build & Push Docker Image

on:
  workflow_dispatch:

  workflow_run:
    workflows: ["Sync fork"]
    types:
      - completed

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if build is needed
        id: changes
        run: |
          # Se il workflow è stato avviato manualmente, forza la build
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "Avvio manuale rilevato. Salto il controllo del diff e forzo la build."
            echo "skip=false" >> $GITHUB_OUTPUT
          else
            # Altrimenti, controlla se ci sono modifiche
            git fetch origin main
            if git diff --quiet HEAD origin/main; then
              echo "skip=true" >> $GITHUB_OUTPUT
            else
              echo "skip=false" >> $GITHUB_OUTPUT
            fi
          fi

      - name: Set up QEMU
        if: steps.changes.outputs.skip == 'false'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: steps.changes.outputs.skip == 'false'
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: steps.changes.outputs.skip == 'false'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Image
        if: steps.changes.outputs.skip == 'false'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.bk
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ghcr.io/alessiocav2004-byte/icvfork:latest
